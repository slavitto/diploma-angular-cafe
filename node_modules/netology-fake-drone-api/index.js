const random = require('./src/random');

exports.deliver = () => {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      const luck = random(0, 100);
      if (luck < 10) {
        return reject();
      }
      return resolve();
    }, random(1500, 5000));
  });
}
